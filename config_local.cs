$cm_config::DB::Connect::server =   "localhost"; // server IP or domain name. Can enter port if it is not default
$cm_config::DB::Connect::user =     "lif_user"; // MUST be a user with ROOT privileges in order to create new DBs
$cm_config::DB::Connect::password = "password"; // password for that user